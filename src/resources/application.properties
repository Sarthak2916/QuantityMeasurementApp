# Database Configuration
db.driver=org.h2.Driver
db.url=jdbc:h2:mem:quantity_measurement;DB_CLOSE_DELAY=-1;MODE=MySQL
db.username=root
db.password=ss108036**
db.pool-size=10

# Pool Settings
db.hikari.maximum-pool-size=10
db.hikari.minimum-idle=5
db.hikari.connection-timeout=30000
db.hikari.idle-timeout=600000
db.hikari.max-lifetime=1800000
db.hikari.pool-name=QuantityPool
db.hikari.connection-test-query=SELECT 1

# Application Environment (development, testing, production)
app.env=development

# Repository Type (cache or database)
repository.type=database

# Logging Configuration
# (We are using java.util.logging as per snippets)